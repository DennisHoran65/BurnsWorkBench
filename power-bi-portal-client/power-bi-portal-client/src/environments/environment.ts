// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  environmentName: 'Local',
  apiUrl: 'https://localhost:44386',
  redirectUri: 'http://localhost:4200/',
  postLogoutRedirectUri: 'http://localhost:4200/',

};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.

     //  redirectUri: 'https://glonlyworkbench.z22.web.core.windows.net/',
     // postLogoutRedirectUri: 'https://glonlyworkbench.z22.web.core.windows.net/',



         // use this to test against client qa
  //private urlRoot = 'https://power-bi-portal-api20200703110839.azurewebsites.net/api/powerbi/';

  // use this to test against gl-only dev
  //private urlRoot       = 'https://burns-workbench-glonly.azurewebsites.net/api/powerbi/';
  
 
     
    